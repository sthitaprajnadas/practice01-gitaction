name: deploy
on:
  push:
    branches:
      - 'main'
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: lint
      run: npm run lint  
    - name: ubuntu-latest
      run: npm run tests  
    - name: ubuntu-latest
      run: npm run build  